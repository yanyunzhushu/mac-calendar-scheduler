# 监控配置

> ⚠️ **占位文件 — 当前不适用**

「日程安排」为纯客户端个人应用，**无运维监控管线**。

当前唯一的"监控"是：
- `scripts/serve.mjs` 的端口健康检测（自动处理端口冲突）
- `@vercel/analytics` 在生产环境加载基础分析（仅页面浏览统计）

如果未来需要监控，此文件将包含：
- 应用性能监控 (APM) 配置
- 错误追踪 (Sentry 等)
- 日志聚合方案
